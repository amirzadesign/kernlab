import './styles/base.css';
import './styles/components.css';

// Placeholder — the real About page content (credit, education section,
// bio) gets built here once the main game loop is working.

const app = document.getElementById('app');

app.innerHTML = `
  <div class="app" style="align-items:center; justify-content:center; text-align:center;">
    <div style="color:var(--color-text-faint); font-size:13px;">
      About page — coming in a later step.
    </div>
  </div>
`;
